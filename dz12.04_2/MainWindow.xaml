<Window x:Class="dz12._04_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Number Statistics" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Content="Generate Numbers" Click="GenerateNumbersButton_Click" Margin="10" />

        <TextBlock Text="Statistics:" Grid.Row="1" Margin="10" TextWrapping="Wrap" />

        <Label Content="Even Numbers Count:" Grid.Row="2" Margin="10, 5, 0, 0" />
        <TextBlock x:Name="EvenNumbersCountTextBlock" Text="{Binding EvenNumbersCount, Mode=OneWay}" Grid.Row="2" Margin="146,10,0,0" />

        <Label Content="Odd Numbers Count:" Grid.Row="2" Margin="300, 5, 0, 0" />
        <TextBlock x:Name="OddNumbersCountTextBlock" Text="{Binding OddNumbersCount, Mode=OneWay}" Grid.Row="2" Margin="426,10,0,0" />
    </Grid>
</Window>