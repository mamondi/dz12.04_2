<Window x:Class="dz12._04_2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Stats App" Height="350" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="filePathTextBox" Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        <TextBlock Text="Enter file path" Grid.Row="0" Margin="5,0" VerticalAlignment="Center" Foreground="Gray">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Text.IsEmpty, ElementName=filePathTextBox}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Button Content="Analyze File" Grid.Row="1" Margin="0,0,0,10" Click="AnalyzeFileButton_Click" HorizontalAlignment="Stretch"/>

        <TextBlock x:Name="sentenceCountTextBlock" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBlock x:Name="uppercaseCountTextBlock" Grid.Row="3" Margin="0,0,0,5"/>
        <TextBlock x:Name="lowercaseCountTextBlock" Grid.Row="4" Margin="0,0,0,5"/>
        <TextBlock x:Name="vowelCountTextBlock" Grid.Row="5" Margin="0,0,0,5"/>
        <TextBlock x:Name="consonantCountTextBlock" Grid.Row="6" Margin="0,0,0,5"/>
        <TextBlock x:Name="digitCountTextBlock" Grid.Row="7" Margin="0,0,0,5"/>
    </Grid>
</Window>
